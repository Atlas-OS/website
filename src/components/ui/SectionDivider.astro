---
interface Props {
  class?: string;
  id?: string;
}

const { class: className = '', id } = Astro.props;
---

<div id={id} class={`border-t border-white/[0.06] pt-8 mt-8 ${className}`}>
  <slot />
</div>

---
interface Props {
  value: string;
  default?: boolean;
}

const { value, default: isDefault = false } = Astro.props;
---

<div data-tab-panel={value} role="tabpanel" hidden={!isDefault}>
  <slot />
</div>

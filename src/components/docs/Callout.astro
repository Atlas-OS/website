---
import { Icon } from 'astro-icon/components';

interface Props {
  type?: 'info' | 'warning' | 'tip' | 'danger';
  title?: string;
  dismissible?: boolean;
}

const { type = 'info', title, dismissible = false } = Astro.props;

const config = {
  info: {
    icon: 'tabler:info-circle',
    accentColor: 'border-l-primary/50',
    iconColor: 'text-primary',
    defaultTitle: 'Info',
  },
  tip: {
    icon: 'tabler:bulb',
    accentColor: 'border-l-primary/50',
    iconColor: 'text-primary',
    defaultTitle: 'Tip',
  },
  warning: {
    icon: 'tabler:alert-triangle',
    accentColor: 'border-l-yellow-500/50',
    iconColor: 'text-yellow-500',
    defaultTitle: 'Warning',
  },
  danger: {
    icon: 'tabler:alert-circle',
    accentColor: 'border-l-red-500/50',
    iconColor: 'text-red-500',
    defaultTitle: 'Important',
  },
};

const style = config[type];
const displayTitle = title || style.defaultTitle;
---

<div
  class={`callout not-prose astro-component callout-animated border-l-[3px] ${style.accentColor} p-5 my-6 border border-[var(--docs-surface-border)] rounded-r-[var(--docs-radius-lg)]`}
  style="background: var(--docs-surface-bg);"
  data-component="callout"
  data-type={type}
>
  <div class="flex items-start gap-3">
    <div class={`callout-icon-wrapper ${style.iconColor} flex-shrink-0`}>
      <Icon name={style.icon} class="mt-0.5 h-5 w-5" aria-hidden="true" />
    </div>
    <div class="min-w-0 flex-1">
      {displayTitle && <p class={`${style.iconColor} mb-2 text-sm font-semibold`}>{displayTitle}</p>}
      <div class="callout-content text-sm leading-relaxed text-white/90">
        <slot />
      </div>
    </div>
    {
      dismissible && (
        <button
          type="button"
          class="callout-dismiss inline-flex size-10 flex-shrink-0 items-center justify-center rounded-[var(--docs-radius-md)] text-white/60 transition-[background-color,color,scale] duration-150 hover:bg-white/[0.05] hover:text-white focus:outline-none focus-visible:ring-2 focus-visible:ring-primary/50 focus-visible:ring-offset-2 active:scale-[0.96]"
          aria-label={`Dismiss ${displayTitle.toLowerCase()}`}
        >
          <Icon name="tabler:x" class="h-4 w-4" aria-hidden="true" />
        </button>
      )
    }
  </div>
</div>

<style is:global>
  .callout {
    margin-top: 1.5rem;
    margin-bottom: 1.5rem;
  }

  .callout-animated {
    animation: callout-fade-in var(--motion-duration-normal, 250ms)
      var(--motion-ease-out, cubic-bezier(0.33, 1, 0.68, 1));
  }

  @keyframes callout-fade-in {
    from {
      opacity: 0;
      transform: translateY(8px);
      filter: blur(4px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
      filter: blur(0);
    }
  }

  .callout-icon-wrapper {
    animation: icon-fade-in 0.6s ease-out;
  }

  @keyframes icon-fade-in {
    0% {
      opacity: 0;
    }
    100% {
      opacity: 1;
    }
  }

  .callout-content p {
    margin-top: 0;
    margin-bottom: 0.5rem;
    color: rgb(255 255 255 / 0.9);
    font-size: 0.875rem;
    line-height: 1.5;
  }

  .callout-content p:last-child {
    margin-bottom: 0;
  }

  .callout-content strong {
    color: white;
    font-weight: 600;
  }

  .callout-content a {
    color: var(--color-primary);
    text-decoration: none;
    font-weight: 500;
    transition: opacity 0.2s ease-in-out;
  }

  .callout-content a:hover {
    opacity: 0.8;
  }

  .callout-content ul,
  .callout-content ol {
    margin-top: 0.5rem;
    margin-bottom: 0.5rem;
    margin-left: 0;
    padding-left: 1.25rem;
    list-style: none;
  }

  .callout-content ol {
    list-style-type: decimal;
    list-style-position: outside;
  }

  .callout-content ul li {
    position: relative;
    margin-bottom: 0.25rem;
    color: rgb(255 255 255 / 0.9);
  }

  .callout-content ul li::before {
    content: '';
    position: absolute;
    left: -1.25rem;
    top: 0.65em;
    width: 0.35em;
    height: 0.35em;
    background: var(--color-primary);
    border-radius: 2px;
  }

  .callout-content ol li {
    margin-bottom: 0.25rem;
    color: rgb(255 255 255 / 0.9);
  }

  .callout-content code {
    background-color: var(--docs-surface-bg);
    border: 1px solid var(--docs-surface-border);
    color: var(--color-primary);
    padding: 0.125rem 0.375rem;
    border-radius: var(--docs-radius-sm);
    font-size: 0.875em;
  }
</style>

<script>
  function setupCalloutDismiss() {
    const dismissButtons = document.querySelectorAll('.callout-dismiss');

    dismissButtons.forEach((button) => {
      if (button instanceof HTMLElement && button.dataset.initialized === 'true') return;
      if (button instanceof HTMLElement) button.dataset.initialized = 'true';
      button.addEventListener('click', () => {
        const callout = button.closest('.callout') as HTMLElement;
        if (callout) {
          callout.style.animation =
            'callout-fade-out var(--motion-duration-normal, 250ms) var(--motion-ease-out, cubic-bezier(0.33, 1, 0.68, 1)) forwards';
          setTimeout(() => {
            callout.remove();
          }, 250);
        }
      });
    });
  }

  document.addEventListener('astro:page-load', setupCalloutDismiss);
</script>

<style is:global>
  @keyframes callout-fade-out {
    from {
      opacity: 1;
      transform: translateY(0);
      filter: blur(0);
    }
    to {
      opacity: 0;
      transform: translateY(-8px);
      filter: blur(4px);
    }
  }
</style>

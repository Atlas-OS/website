---
title: Windows settings
metaDescription: "AtlasOS shortcuts to Windows Settings: activation, colors, date, apps, region, taskbar."
description: Shortcuts to Windows Settings pages.
order: 25
---

import AtlasFolderRestoreLink from '@/components/docs/AtlasFolderRestoreLink.astro';
import Callout from '@/components/docs/Callout.astro';
import CodeInline from '@/components/docs/CodeInline.astro';

# Windows settings

Run these shortcuts to open common Windows settings. Some settings remain locked until you activate Windows.

Open <CodeInline text="5. Windows Settings" /> in the Atlas folder on your Desktop. If the Atlas folder is missing, follow{' '}<AtlasFolderRestoreLink />.

Each file opens its matching Windows Settings page.

## Activation

Run the Activation file to open Windows activation settings. Activate Windows before you change settings that require a license.

AtlasOS requires a valid Windows license. Activate Windows with a legitimate license before installation. Activation is a one-time process.

<Callout type="warning" title="Atlas is Not Pre-activated">
  AtlasOS is **not** a pre-activated version of Windows. You must activate Windows with a valid,
  legitimate license before installation. When purchasing a Windows license, confirm the seller is
  trusted and the license is legitimate.
</Callout>

## Shortcut reference

Each file opens the corresponding Windows Settings page:

| File | Opens |
|------|-------|
| **Colors** | Color and styling options |
| **Date & Time** | Time, date, and time zone settings |
| **Default apps** | Default apps for file types (e.g., browser, media player) |
| **Default Graphics Settings (HAGS)** | Hardware-accelerated GPU scheduling and default graphics preferences |
| **Desktop icons** | Desktop icon visibility (This PC, Recycle Bin, etc.) |
| **Language** | Display and input language |
| **Privacy** | Privacy and diagnostic settings |
| **Region** | Country and region settings |
| **Taskbar** | Taskbar customization |

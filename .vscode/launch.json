{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "bun run dev",
      "name": "Astro Dev Server (Bun)",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    },
    {
      "command": "bun run build",
      "name": "Build (Bun)",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    },
    {
      "command": "bun run preview",
      "name": "Preview (Bun)",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
